<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.augustanasi.vickingstudyroomexpress">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Viking Study Rooms"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Transition"></activity>
        <activity android:name=".RoomListActivity"></activity>
        <activity android:name=".tempDest"></activity>
        <activity android:name=".AccessLevelChanger"></activity>
        <activity android:name=".CheckInOut"></activity>
        <activity android:name=".ChangeRoomState"></activity>

    </application>

</manifest>